import * as ajv from "ajv";
import { CustomKeyword, ValidatorError, ValidatorOptions } from "./validator.extras";
export declare class ValidatorService {
    private static _instance;
    private _ajv;
    private readonly BASE_SCHEMA_PATH;
    private constructor();
    static init(options: ValidatorOptions): ValidatorService;
    getBaseSchemaPath(): string;
    validate(schema: object | string | boolean, data: any): boolean | PromiseLike<any>;
    formatErrors(errors: ajv.ErrorObject[]): ValidatorError[];
    getErrors(): ValidatorError[];
    registerKeywords(keywords: CustomKeyword[]): void;
}
