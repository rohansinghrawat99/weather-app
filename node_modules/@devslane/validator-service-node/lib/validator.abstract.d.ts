import { ValidatorService } from "./validator.service";
export declare abstract class ValidatorAbstract {
    protected abstract getSchemaName(): string;
    protected abstract getValidatorInstance(): ValidatorService;
    validate(data: any): Promise<undefined>;
}
